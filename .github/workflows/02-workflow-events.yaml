name: 02 - Workflow Events
on: 
  # push
  schedule:
    - cron: '0 0 * * * '
jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Show the trigger
        run: |
          echo "trigger- ${{ github.event_name}}"
          